-- The full fictional artist configuration is inserted by `npm run setup:local` after
-- migrations complete. Keeping the JSON source in content/demo/bootstrap-config.json
-- gives the repository and the deterministic setup script one bootstrap authority.
select 1;
